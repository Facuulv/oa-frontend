/** localStorage key and mirrored cookie name (for Next.js middleware). */
export const AUTH_TOKEN_KEY = "oa-auth-token";

/** Max-Age for the auth cookie (seconds). */
export const AUTH_TOKEN_COOKIE_MAX_AGE = 60 * 60 * 24 * 7;
